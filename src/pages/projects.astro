---
import ProjectCard from "../components/ProjectCard.astro";
import BaseLayout from "../layouts/BaseLayout.astro";

interface Projects {
  repository: string;
  description: string;
}

const projects: Projects[] = [
  {
    repository: "runtipi/runtipi",
    description:
      "Runtipi is a homeserver for everyone! One command setup, one click installs for your favorites self-hosted apps.",
  },
  {
    repository: "steveiliop56/tinyauth",
    description: "The simplest way to protect your apps with a login screen.",
  },
  {
    repository: "steveiliop56/tipimate",
    description:
      "The easiest way to get notified about updates for your Runtipi apps!",
  },
  {
    repository: "steveiliop56/steveiliop56.github.io",
    description: "My personal website.",
  },
];
---

<BaseLayout title="Projects">
  <div class="flex flex-col flex-1 p-4 justify-center items-center">
    <div class="flex flex-col gap-4 items-stretch">
      <p class="text-4xl font-bold">Projects</p>
      <p>Here are my favorite projects that I have built/worked on:</p>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        {projects.map((project) => <ProjectCard {...project} />)}
      </div>
    </div>
  </div>
</BaseLayout>
